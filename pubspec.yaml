name: cutils
description: "Flutter Utils."
version: 0.1.0
homepage: https://github.com/BIG-BEARC/cutils
repository: https://github.com/BIG-BEARC/cutils
environment:
  sdk: ^3.6.2
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2 #数据持久化
  uuid: ^4.5.0
  logger: ^2.6.2 #log 日志
  connectivity_plus: ^6.1.4 # 网络连接库
  decimal: ^3.0.2 #计算不损失精度库，可以表示一个任意大小且精度完全准确的浮点数。类似BigDecimal
  dartx: ^1.2.0 #file_utils 的 isNullOrEmpty/.filter 使用
  file: ^7.0.0 #文件系统库
  path: ^1.9.0 #路劲操作库
  path_provider: ^2.1.5 #获取常用文件路径库
  archive: ^3.3.7 #压缩工具
  package_info_plus: ^8.3.0 # 包信息
  device_info_plus: ^11.3.0 #设备信息
  event_bus: ^2.0.1 #事件分发库
  crypto: ^3.0.7
  convert: ^3.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  test: ^1.24.0
  mocktail: ^1.0.4
  fake_async: ^1.3.1
  path_provider_platform_interface: ^2.0.0
  plugin_platform_interface: ^2.0.2 #file_utils_test 的 MockPlatformInterfaceMixin 使用
# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # This section identifies this Flutter project as a plugin project.
  # The 'pluginClass' specifies the class (in Java, Kotlin, Swift, Objective-C, etc.)
  # which should be registered in the plugin registry. This is required for
  # using method channels.
  # The Android 'package' specifies package in which the registered class is.
  # This is required for using method channels on Android.
  # The 'ffiPlugin' specifies that native code should be built and bundled.
  # This is required for using `dart:ffi`.
  # All these are used by the tooling to maintain consistency when
  # adding or updating assets for this project.

  # To add assets to your plugin package, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  #
  # For details regarding assets in packages, see
  # https://flutter.dev/to/asset-from-package
  #
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # To add custom fonts to your plugin package, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts in packages, see
  # https://flutter.dev/to/font-from-package
